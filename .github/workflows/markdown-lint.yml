name: Markdown Lint
on:
  pull_request:
    paths: ['**/*.md']
  push:
    branches: [main]
    paths: ['**/*.md']
permissions:
  contents: read
jobs:
  lint:
    uses: JacobPEvans-personal/.github/.github/workflows/_markdown-lint.yml@main
